import {Tile} from "../internals/tile.ts";
import {CanvasNode} from "../internals/canvas_node.ts";

export class Floor extends CanvasNode {
    // render(ctx: CanvasRenderingContext2D){
    //     ctx.fillStyle = "white";
    //     ctx.fillRect(this.x * this.tile_size, this.y * this.tile_size, this.tile_size, this.tile_size);
    //     ctx.strokeStyle = "black";
    //     ctx.strokeRect(this.x, this.y, this.tile_size, this.tile_size)
    //     this.ctx.strokeRect(this.x * this.tile_size, this.y * this.tile_size, this.tile_size, this.tile_size);
    // };
}